#include <stdio.h>

int main(){
    int number;
    number = 11;
    printf("Value stores in variable number : %d\n",number);
    printf("size of integer data type is : %lu\n",sizeof(number));
    return 0;
}